# Prevent electron and related packages from being hoisted to root
# This is required for electron-vite to work correctly in a monorepo
public-hoist-pattern[]=!electron
public-hoist-pattern[]=!electron-*
